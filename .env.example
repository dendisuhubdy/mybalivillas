# =============================================================================
# MyBaliVilla.com - Environment Variables Template
#
# Copy this file to .env.prod for production deployment:
#   cp .env.example .env.prod
#
# For local development, docker-compose.yml has built-in defaults.
# =============================================================================

# ---------------------------------------------------------------------------
# PostgreSQL Database
# ---------------------------------------------------------------------------
POSTGRES_DB=mybalivilla
POSTGRES_USER=mybalivilla
# IMPORTANT: Generate a strong password for production!
# Example: openssl rand -base64 32
POSTGRES_PASSWORD=CHANGE_ME_TO_A_STRONG_PASSWORD

# ---------------------------------------------------------------------------
# Redis
# ---------------------------------------------------------------------------
# IMPORTANT: Generate a strong password for production!
# Example: openssl rand -base64 32
REDIS_PASSWORD=CHANGE_ME_TO_A_STRONG_PASSWORD

# ---------------------------------------------------------------------------
# JWT Authentication
# ---------------------------------------------------------------------------
# IMPORTANT: Generate a strong secret for production!
# Example: openssl rand -base64 64
JWT_SECRET=CHANGE_ME_TO_A_STRONG_SECRET

# JWT token expiry in hours (default: 24)
JWT_EXPIRY_HOURS=24

# ---------------------------------------------------------------------------
# Application Logging
# ---------------------------------------------------------------------------
# Options: trace, debug, info, warn, error
RUST_LOG=info

# ---------------------------------------------------------------------------
# CORS (Cross-Origin Resource Sharing)
# ---------------------------------------------------------------------------
# Comma-separated list of allowed origins
# For production, set to your actual domain
CORS_ORIGINS=https://mybali.villas,https://www.mybali.villas,https://mybali.casa,https://www.mybali.casa

# ---------------------------------------------------------------------------
# Frontend URLs (used by Next.js at build time)
# ---------------------------------------------------------------------------
# Public API URL as seen by the browser
NEXT_PUBLIC_API_URL=https://mybali.villas/api/v1

# Admin API URL as seen by the admin panel browser
NEXT_PUBLIC_ADMIN_API_URL=https://mybali.villas/api/admin

# ---------------------------------------------------------------------------
# Domain Configuration
# ---------------------------------------------------------------------------
DOMAIN=mybali.villas

# ---------------------------------------------------------------------------
# DigitalOcean Spaces (for image uploads and backups)
# ---------------------------------------------------------------------------
# DO_SPACES_KEY=your_spaces_access_key
# DO_SPACES_SECRET=your_spaces_secret_key
# DO_SPACES_BUCKET=mybalivilla-assets
# DO_SPACES_REGION=sgp1
# DO_SPACES_ENDPOINT=https://sgp1.digitaloceanspaces.com

# ---------------------------------------------------------------------------
# Email (for inquiry notifications)
# ---------------------------------------------------------------------------
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=notifications@mybalivilla.com
# SMTP_PASSWORD=your_email_password
# SMTP_FROM=MyBaliVilla <notifications@mybalivilla.com>

# ---------------------------------------------------------------------------
# Image Upload Configuration
# ---------------------------------------------------------------------------
# Maximum upload size in MB
# MAX_UPLOAD_SIZE_MB=10
# Allowed image formats (comma-separated)
# ALLOWED_IMAGE_FORMATS=jpg,jpeg,png,webp,avif
